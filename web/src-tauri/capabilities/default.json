{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the POS window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
